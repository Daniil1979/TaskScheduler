# TaskScheduler — REST API для управления задачами

Это учебный проект на Spring Boot, реализующий CRUD-операции для управления списком задач (To-Do List) через REST API.

## CI/CD Pipeline

Проект включает полноценный CI/CD пайплайн, настроенный через **GitHub Actions**:

- Сборка и тестирование с помощью Maven
- Сборка Docker-образа
- Публикация образа в Docker Hub (`daniil79/task-scheduler`)
- Автоматический деплой на удалённую виртуальную машину

Приложение доступно по адресу:  
🔗 **http://158.160.187.61:8080/tasks**

---

## Почему развёртывание выполнено в облаке?

Изначально проект тестировался на локальной виртуальной машине (VirtualBox), однако для корректной работы **автоматического деплоя через GitHub Actions** требуется, чтобы целевой сервер был **доступен из интернета**.

Локальная сеть (включая домашний Wi-Fi) использует **приватные IP-адреса**, которые **недоступны извне**.  
Кроме того, как выяснилось мой интернет-провайдер применяет **CGNAT**, что делает невозможным проброс портов и входящие подключения к локальному компьютеру.

Поэтому для обеспечения стабильной и автоматизированной доставки приложения был выбран облачный провайдер — **Yandex Cloud**, где:
- Виртуальная машина имеет **публичный IP-адрес**
- GitHub Actions может подключиться по SSH
- Приложение становится доступно глобально без дополнительных настроек

Считаю такой подход соответствует **заданию** и демонстрирует реалистичный сценарий развёртывания веб-приложений.

---

## Технологии

- **Backend**: Java 21, Spring Boot 3.4.0
- **Сборка**: Maven
- **Контейнеризация**: Docker
- **CI/CD**: GitHub Actions
- **Инфраструктура**: Yandex Cloud (Ubuntu 22.04 LTS)
- **Репозиторий образов**: Docker Hub

---